package com.ciareader.reader

import org.junit.Assert.assertTrue
import org.junit.Test

/**
 * Placeholder so the unit-test source set + `testDebugUnitTest` task exist
 * from day one. Real tests ship with each phase's logic (per repo policy:
 * every ticket ships tests + a coverage floor).
 */
class ScaffoldSanityTest {
    @Test
    fun scaffoldIsWiredUp() {
        assertTrue(true)
    }
}
